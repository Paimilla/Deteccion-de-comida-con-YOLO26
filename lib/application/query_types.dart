class BarcodeQuery {
  final String code;

  const BarcodeQuery(this.code);
}

class FoodTextQuery {
  final String textEs;

  const FoodTextQuery(this.textEs);
}

class RecipeQuery {
  final String ingredientEs;

  const RecipeQuery(this.ingredientEs);
}

class VisionQuery {
  final String imagePath;

  const VisionQuery(this.imagePath);
}
